//definindo as rotas da API
//usando enum para definir constantes nomeadas
//enum (enumeration) é um recurso do TypeScript que permite definir um conjunto de constantes nomeadas
//cada constante representa uma rota específica da API


export enum Routes {
    list = '/podcasts',
    filter = '/podcasts/filter'
}